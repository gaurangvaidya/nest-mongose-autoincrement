export const requiredText = (field: string): string => {
  return `${field} is required`;
};
export const notValidText = (field: string): string => {
  return `${field} is not valid`;
};

export const notFoundText = (field: string): string => {
  return `${field} not found`;
};
